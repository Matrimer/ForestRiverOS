# System-packages. Removing these will likely break the system

- type: dnf
  install:
    packages:

      - curl
      - just

      - pipewire
      #- pipewire-module-x11
      - pipewire-plugin-vulkan
      - pipewire-pulseaudio
      - pipewire-alsa
      - pipewire-utils
      - wireplumber
      - alsa-firmware

      # # Firewall
      # - nftables
      # - firewalld
      - openssl
      # # Printing
      # - cups

      # - iwd
      # - NetworkManager
      - NetworkManager-wifi
      - NetworkManager-openvpn
      - NetworkManager-openconnect
      - NetworkManager-openvpn-gnome
      - NetworkManager-openconnect-gnome
      - bluez
      - bluez-tools

      - tuned
      - tuned-ppdi
      - upowerd
      
      - greetd
      - greetd-selinux

      - qt5-qtwayland
      - qt6-qtwayland
      - xorg-x11-server-Xwayland

      - polkit
      - xfce-polkit
      - ublue-polkit-rules
      - pam
      - fprintd-pam
      - gnome-keyring

      - android-tools
      - gvfs-mtp
      - gvfs-gphoto2
      - gvfs-smb
      - gvfs-nfs
      - gvfs-fuse
      - gvfs-archive


      - linux-firmware

      - intel-ucode
      - amd-ucode
      - intel-gpu-firmware
      - amd-gpu-firmware
      - linux-firmware
      - microcode_ctl
      - vulkan-validation-layers
      - vulkan-tools


      - dbus-tools
      - dbus-daemon


      
# GROUPS
# container-management
# C-development
# electronic-lab
# engineering-and-scientific
#
# HIDDEN:
# core *
# java
# java-development
# basic-desktop
# base-graphical
# autoring-and-publishing
# multimedia *
# fonts
# hardware-support
# standard
# smb-server
# security-lab
# printing *
# system-tools
# workstation-product
# networkmanager-submodules
